import { css } from '@emotion/css'
import colors from "../../../docs/foundations/colors";
import spacing from "../../../docs/foundations/spacing";
import typography from "../../../docs/foundations/typography";
import Playground from "../../../docs/playground";
import Button from "../../../src/components/button";
const rootCss = css`
    li {
      list-style: none;
      display: grid;
      grid-template-columns: 12px 240px 200px;
      border-bottom: 1px solid ${colors.gray200};
      padding: 4px 0;
      &:hover {
        background-color: ${colors.primary1};
      }
      div {
        &:first-child {
        }
      }
    }
`;
const formCss = css`
  width: 100%;
  .form {
    border-radius: 4px;
    border: 1px solid ${colors.gray200};
    background-color: ${colors.formBg};
    padding: ${spacing.paddingForm};
  }
  .section-header {
    color: ${colors.black};
    ${typography.h5}
  }
  .form-section {
    padding: ${spacing.paddingSection};
    margin: ${spacing.marginSection};
    background-color: ${colors.formSectionBg};
  }
  .form-subsection {
    padding: ${spacing.paddingSubsection};
    margin: ${spacing.marginSubsection};
    background-color: ${colors.formSubsectionBg};
  }
  .subsection-header {
    color: ${colors.black};
    ${typography.h6};
  }
  .form-action-buttons {
    margin: ${spacing.marginActionButtons};
  }
`;

# Spacing

#### import colors from "/src/shared/spacing";

<Playground
  scope={{ spacing, rootCss }}
  code={`
    <ul className={rootCss}>
      {
        Object.entries(spacing).map(entry => (
          <li>
            <div style={{ backgroundColor: entry[1] }}></div>
            <div>spacing.{entry[0]}</div>
            <div>{entry[1]}</div>
          </li>
        ))
      }
    </ul>
  `}
/>

# Form Layout

<Playground
  scope={{ formCss, Button }}
  code={`
    <div className={formCss}>
      {
      <div className="form">
        <div className="section-header">
          Section Header
        </div>
        <div className="form-section" >
          <div className="form-subsection" >
            <div className="subsection-header">
              Sub-Section Header
            </div>
          </div>
          <div className="form-subsection" >
            <div className="subsection-header">
              Sub-Section Header
            </div>
          </div>
        </div>
        <div className="section-header">
          Section Header
        </div>
        <div className="form-section" >
          <div className="form-subsection" >
            <div className="subsection-header">
              Sub-Section Header
            </div>
          </div>
          <div className="form-subsection" >
            <div className="subsection-header">
              Sub-Section Header
            </div>
          </div>
        </div>
        <div className="form-action-buttons" >
          <Button>Button</Button>
        </div>
      </div>
      }
    </div>
  `}
/>
