import Badge from "../../src/components/Badge";
import Playground from "../../docs/playground";

# Default

The default form of a badge.

<Playground
  scope={{ Badge }}
  code={`<Badge>{8}</Badge>`}
/>

# Primary

Use a primary badge to help draw attention to new or updated information.

<Playground
  scope={{ Badge }}
  code={`<Badge appearance="primary">{42}</Badge>`}
/>

# Important

Use an important badge to call attention to information that needs to stand out.

<Playground
  scope={{ Badge }}
  code={`<Badge appearance="important">{42}</Badge>`}
/>
