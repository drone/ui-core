import Textarea from "../../src/components/Textarea";
import Playground from "../../docs/playground";

# Textarea

The default form of a text area.

<Playground
  scope={{ Textarea }}
  code={`<Textarea placeholder="Enter a description …" spellcheck={"off"} />`}
/>

# Disabled

Use the disabled attribute to disable the input:

<Playground
  scope={{ Textarea }}
  code={`<Textarea disabled={true} />`}
/>
